export default class Currency {
  constructor(code, name) {
    /* eslint-disable-next-line no-underscore-dangle */
    this._code = code;
    /* eslint-disable-next-line no-underscore-dangle */
    this._name = name;
  }
}
